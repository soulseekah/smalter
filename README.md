Add execution tracing to .smali code by injecting `Log->println` calls into each and every method (almost).

Usage: `smalter.py <classes dir> [classes dir] [classes dir] ...`

smali 'em back up and watch the logs ;) shhh...
